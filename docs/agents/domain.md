# Domain Docs

Before changing Agent Run runtime behavior, read `docs/agents/runtime-boundary.md`.

This repository owns Python LangGraph runtime behavior and direct runtime experiments. It does not
own TypeScript product API behavior, browser-facing stream compatibility, or whole-product path
measurement through `POST /api/agent-runs`.
